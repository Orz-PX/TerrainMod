using System;
using spaar.ModLoader;
using System.Collections;
using UnityEngine;
using UnityEngine.SceneManagement;

namespace TerrainMod
{
    class TerrainDeformer : MonoBehaviour
    {        
        void Update()
        {
            //float[,] heights = new float[1, 1];
            //heights[0, 0] = 0.7f;
            //if (StatMaster.isSimulating)
            //{
            //    foreach (var terrain in Terrain.activeTerrains)
            //    {
            //        terrain.terrainData.SetHeights(10, 10, heights);
            //    }
            //    //Terrain.activeTerrains[2].terrainData.SetHeights(10, 10, heights);
            //    //Debug.Log(Terrain.activeTerrains.Length);
            //}
        }
    }
}